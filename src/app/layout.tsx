import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});


const appUrl = process.env.NEXT_PUBLIC_URL || "https://football-prediction-nqiy7o1zq-nobodys-projects-a3ac7cbf.vercel.app";

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
  other: {
    "fc:frame": JSON.stringify({
      version: "next",
      imageUrl: `${appUrl}/globe.svg`, // Using existing public globe.svg
      button: {
        title: "Predict Matches",
        action: {
          type: "launch_frame",
          name: "Laliga Predictor",
          url: appUrl,
          splashImageUrl: `${appUrl}/globe.svg`,
          splashBackgroundColor: "#ffffff",
        },
      },
    }),
  },
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
